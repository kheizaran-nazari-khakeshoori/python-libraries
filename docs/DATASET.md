# Dataset Guidance

Use a historical climate dataset such as "Global Land Temperatures by City" from Kaggle.
The CSV should include a date or year column and a temperature field such as `average_temperature`.

Place the CSV in a local `data/` directory and pass the path to `run_analysis.py --data`.
